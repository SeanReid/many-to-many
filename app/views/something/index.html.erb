<h1>Same Cities In Different States</h1>
<p>This is a few cities that share the same name and showing what states they are located in.</p>
  <% @cities.each do |city| %>
    <%= city.name %>
    <ul>
      <% city.states.each do |state| %>
        <li><%= state.name %></li>
      <%end%>
    </ul>
<%end%>
